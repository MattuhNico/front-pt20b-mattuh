import LoginForm from "@/components/LoginForm/LoginForm";

const page = () => {
  return (
    <main className="container text-center">
      <h1>Login</h1>
      <LoginForm />
    </main>
  );
};

export default page;
